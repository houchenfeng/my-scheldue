# 个人日程管理系统 PRD(v1)

> 本文件是后续开发的**唯一依据**。需求如有变更,先更新本文件再编码。
> 状态:已确认(需求提问 → 概念图 → 页面设计,均已由用户确认)
> 确认日期:2026-09-25

## 1. 产品目标和使用场景

**目标**:为大学生提供一个单用户的个人日程管理工具,统一管理「课程」(每周固定安排)与「日程活动」(自由待办),首页快速总览今日安排。

**使用场景**:
- 每天出门前/开始一天时:打开首页看今天有什么课(时间、教室、教师)和今天要做的待办。
- 查看本周或任意教学周的课程安排与教室。
- 随时添加/编辑/删除日程活动,设置提醒与重复。
- 关闭浏览器后下次进入:所有数据依旧可见,并停留在上次所在页面。

**产品约束**:
- 仅个人使用,**不做用户注册登录**。
- 第一版**不做任何社交功能**。
- 纯前端 Web 应用(浏览器直接打开 index.html),无后端;数据存浏览器 localStorage。

## 2. 整体页面框架和导航结构

形态:纯前端,文件规划 `index.html` / `style.css` / `app.js`。
存储:localStorage,key = `my-schedule.v1`。
路由:hash 路由 `#/home`、`#/timetable`、`#/todos`,默认 `#/home`;刷新或重新打开停留在当前页。

```
┌──────────────────────────────────────────────────────────────┐
│ [G1] 顶栏  我的日程  2026—2027学年 第一学期 · 第3周 · 09-25 周五  [⚙ 设置] │
│ [G2] 导航  ( 首页 ) ( 课表 ) ( 日程活动 )   ← ≤768px 变底部固定标签栏   │
│ [G3] 提醒条 ⏰ 组会讨论 15 分钟后开始 [跳转] [×]  ← 无提醒时整条隐藏     │
├──────────────────────────────────────────────────────────────┤
│ [G4] 页面内容区(三个页面在此切换,各自独立滚动)                      │
└──────────────────────────────────────────────────────────────┘
```

- 全局 tick:每 30 秒刷新提醒条、课程状态(未开始/上课中/已下课)、过期标记、顶栏周次。
- 视觉:浅色主题、卡片圆角布局;主色蓝;课程按录入顺序取 6 色循环色板;优先级 高=红 / 中=橙 / 低=灰。
- 全局弹窗 4 个:M1 新建/编辑活动、M2 课程详情(只读)、M3 设置、M4 通用二次确认。

## 3. 首页的内容结构

```
[A] 日期卡:今天 2026-09-25 · 星期五 · 第一学期 第 3 周
    (今天不在第 1~19 周 → 改显示"假期中 · 非教学周")
[B] 今日课程(左栏):按开始时间升序
    行 = 时间范围 · 第X-Y节 · 课程名称 · 地点 · 教师 + 状态标(未开始 / 上课中蓝底 / 已下课灰)
    点行 → M2;空态:"今天没有课"
[C] 今日待办(右栏) [+ 新建]
    复选框切换"今天"的完成状态(划线灰);点行 → M1 编辑;[+ 新建] → M1 且日期预填今天
    过期未完成 → 行首红标"过期";空态:"今天暂无待办"
[D] 本周概览:周一~周日 7 格,每格 = 日期 + 课块数 + 未完成待办数,今天描边;仅展示不可点
```

≤768px 时 [B][C] 改单栏堆叠。

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| 首页 `#/home` | 聚合展示「今日计划」(今日课程 + 今日待办)与本周概览,提供快捷新建入口 |
| 课表 `#/timetable` | 以周为单位网格展示课程安排,切换任意教学周,查看课程完整信息 |
| 日程活动 `#/todos` | 待办全生命周期管理:新建/编辑/删除/完成/筛选/重复/提醒设置 |
| 设置 M3 | 维护节次作息时间;导出/导入/清空数据 |
| 提醒系统 G3 | 对有时间设置的待办做到时提醒(提醒条 + 浏览器 Notification)与过期标红 |

## 5. 每个模块第一版最必要的数据和操作

### 5.1 数据模型(localStorage `my-schedule.v1`)

```
meta    { term:"2026—2027学年 第一学期", week1Monday:"2026-09-07", totalWeeks:19 }
periods [ {n:1,start:"08:00",end:"08:45"} … {n:14,start:"21:30",end:"22:15"} ]   M3 可编辑
courses [ {code,name,class,campus,unit,mode,teacher,count,firstDate,note,
           slots:[{wd:1..7, p:[起节,止节], weeks:[起周,止周], loc}] } ]
todos   [ {id,title,date,time(可空=全天),loc,priority:"高|中|低",
           remind:{on,before:5|10|15|30|60}, repeat:"none|daily|weekly",
           note, doneDates:["YYYY-MM-DD"], createdAt} ]
```

当前周 = floor((今天 - week1Monday) / 7天) + 1。

### 5.2 课程种子数据(逐字照抄用户课表截图,首次启动内置;v1 只读不可增改)

| 课程名称 | 课程代码 | 周次·星期·节次 | 地点 | 任课教师 | 上课方式 |
|---|---|---|---|---|---|
| 可靠性中的机器学习(博) | D141011A03 | 1-9周 周一 [1-2节] | B118 | 刘杰 | 面授讲课 |
| 人工智能安全与伦理 | D411026B01 | 无上课时间 | — | 韦星星 | 全线上教学 |
| 科学研究与学术写作 | D571026C31 | 1-8周 周五 [2-5节] | 计算机房(R1-4093) | 黄雪飞,张莹莹 | 面授讲课 |
| 物流运输模型与算法 | T130032042 | 6-11周 周一 [8-9节];6-10周 周一 [10-10节];6-10周 周二 [8-10节] | 教学一号楼3003 | 周宇 | 面授讲课 |

其余字段同样逐字内置:班级名称(1 / 01 / 01 / 国新院班)、校区(学院路校区 / 学院路校区 / 杭州国际创新研究院 / 杭州国际创新研究院)、开课单位、上课人数、首次上课日期(2026-09-07 / 空 / 2026-09-11 / 2026-10-12)、备注(线上上课 / 线上上课 / 线下上课 / 线下上课),在 M2 中展示。

### 5.3 节次种子(逐字照抄校历教学作息时间表)

1 08:00-08:45;2 08:50-09:35;3 09:50-10:35;4 10:40-11:25;5 11:30-12:15;
6 14:00-14:45;7 14:50-15:35;8 15:50-16:35;9 16:40-17:25;10 17:30-18:15;
11 19:00-19:45;12 19:50-20:35;13 20:40-21:25;14 21:30-22:15。

### 5.4 各模块必要操作

- **首页**:查看今日课程/待办;勾选待办完成;[+ 新建]打开 M1(日期=今天);点待办行打开 M1 编辑;点课程行打开 M2。
- **课表**:周切换(限 1~19,边界禁用箭头,非当前周显示[回到本周]);课块按「查看周 ∈ 课程周次范围」绘制,跨行 = 止节-起节+1,块内显示课程名+地点;同一格多块左右均分;点课块或无固定时间芯片 → M2;无固定时间课程(人工智能安全与伦理)固定列在网格下方;表头行与节次列吸顶/吸左,移动端整表横向滚动。
- **日程活动**:筛选 [全部|今天|本周|未完成|已完成] 单选;新建/编辑走 M1;删除走 M4 二次确认后删除(重复活动删整个系列);复选框切换该日期的 doneDates;按日期升序分组,过去日期存在未完成时组头红标「N 项过期」;组内按时间升序、无时间排最前;重复 daily/weekly 按规则命中相应日期,完成按天记录。
- **M1 字段与校验**:标题*、日期*、时间(可空=全天)、地点、优先级(分段选择,默认中)、提醒(开关+提前 5/10/15/30/60 分钟,默认开·15)、重复(无/每天/每周,默认无)、备注;标题或日期为空 → 输入框下红字提示并聚焦,不保存。
- **M3**:14 节起止时间编辑(time 输入,改完即存);学期信息只读;导出 JSON(下载备份文件);导入 JSON(校验失败红字提示且不覆盖原数据,成功需确认覆盖);清空全部数据(M4 二次确认,执行后回到初始状态:种子课程+种子节次+空待办)。
- **提醒系统**(30 秒 tick,对象 = 今天命中 且 有时间 且 当天未完成的待办):

```
now < 开始-提前量           → 静默
开始-提前量 ≤ now ≤ 开始    → 激活:G3 提醒条 + 浏览器 Notification(每条 occurrence 仅弹一次,首次触发请求授权)
now > 开始                 → 过期:首页/列表红标"过期",不再弹通知
提醒条 [×] → 隐藏该条本次(记 todoId@date);点正文 → 跳 #/todos 并高亮该行 2 秒
无时间的待办:不参与提醒与过期,仅按日期出现在列表
限制:页面关闭时不提醒(纯前端固有限制,已确认)
```

## 6. 首页、今日计划和各模块之间的关系

- 「今日计划」不是独立模块,即首页 [B] 今日课程 + [C] 今日待办 的聚合区。
- **数据单一来源**:courses 与 todos 在 `my-schedule.v1` 中各存一份;首页不存自己的数据,全部实时派生:
  - 今日课程 = courses 中 slot.wd = 今天星期 且 当前周 ∈ slot.weeks 的课,用 periods 换算起止时间后按开始时间排序。
  - 今日待办 = todos 中 date = 今天,或 repeat=daily,或 repeat=weekly 且星期相同;完成态 = doneDates 是否含今天。
- **入口复用**:首页 [+ 新建] 与点待办行,复用日程活动的 M1 表单并写同一份 todos;首页点课程行复用课表的 M2。改一处,处处同步。
- **共享基础数据**:课表与首页共用 meta/periods/courses;M3 修改节次时间后,两页的时间显示同时更新。
- **提醒系统**扫描 todos,结果体现在所有页面的 G3 提醒条,以及首页/列表的过期红标。

## 7. 第一版必须完成的功能

1. 三个页面 + hash 路由 + 刷新/重开停留当前页。
2. 课程种子(5.2)、节次与学期种子(5.1/5.3)首次启动内置。
3. 课表网格:周切换、按周次显隐课块、课块跨节、课程详情 M2、无固定时间课程区。
4. 待办 CRUD + 完成(重复活动按天记)+ 5 种筛选 + 重复(无/每天/每周)+ 优先级 + 提醒设置。
5. 首页:日期卡(周次计算、假期显示)、今日课程(状态标)、今日待办、本周概览、各空态。
6. 提醒系统:提醒条 + 浏览器 Notification + 过期红标 + 30 秒 tick。
7. 设置:节次时间编辑、导出 JSON、导入 JSON、清空全部数据。
8. localStorage 持久化:关闭浏览器再打开数据依旧。
9. 响应式 ≤768px:底部标签栏、首页单栏、课表横向滚动。
10. 表单校验与全部二次确认(删除/清空/导入覆盖)。

## 8. 第一版暂时不做的功能

- 用户注册登录、多用户、社交功能。
- 后端、云同步、多设备同步。
- 页面关闭时的提醒(后台推送 / Service Worker / 服务端推送)。
- 节假日识别与调课(国庆、中秋等校历红字日期不影响课表排布)。
- 待办文本搜索。
- 重复活动的单次例外(单独改/删某一次)。
- 首页 [D] 本周概览点击进日视图。
- 课程的手动增删改(v1 课程为只读种子)。
- 深色主题、数据统计报表、多学期切换。

## 9. 可以实际检查的验收标准

验收方式:浏览器打开 index.html,逐条实际操作检查。

1. 打开即见首页 [A] 显示今天的日期、星期与正确周次(以 2026-09-25 为例 = 第一学期 第 3 周);周次由 week1Monday 自动推算。
2. 周五打开首页 [B]:出现「科学研究与学术写作 08:50-12:15 计算机房(R1-4093) 黄雪飞,张莹莹」;周一(第 1-9 周)出现「可靠性中的机器学习(博) 08:00-09:35 B118」。
3. 课表页切换周次:第 10 周周一「可靠性中的机器学习(博)」消失、「物流运输模型与算法」出现;第 12 周周一物流 [8-9节] 块消失(仅到 11 周);第 19 周时 ▶ 禁用、第 1 周时 ◀ 禁用。
4. 课表网格下方显示「人工智能安全与伦理 · 全线上教学」;点击弹出 M2,内含课程代码 D411026B01、教师 韦星星。
5. 日程活动新建「组会讨论」(日期=今天、时间 14:00、提醒开):列表今日分组与首页 [C] 同时出现;编辑改标题后两处同步更新;删除弹 M4 确认后两处同时消失。
6. 持久化:新建待办后 F5 或关闭浏览器重开,待办仍在;刷新后停留在刷新前的页面。
7. 重复:新建 repeat=每周 的活动,下周同日期仍出现;勾选本周当天的完成,下周那天仍为未完成。
8. 提醒:新建开始时间为 2 分钟后、提前 1 分钟提醒的活动,30 秒内 G3 提醒条出现,授权后浏览器 Notification 弹出且仅一次;把开始时间改为已过去,该行显示红标「过期」。
9. 备份链路:改一节节次时间 + 新建 2 条待办 → 导出 JSON;清空全部数据 → 回到初始状态(待办空、节次为种子);导入刚才的文件 → 待办与节次修改全部恢复;导入非法文件 → 红字提示且原数据不变。
10. M3 把第 1 节开始改为 08:10:首页/课表中「可靠性中的机器学习(博)」显示时间变为 08:10-09:35。
11. 浏览器窗口宽 375px:导航变底部标签栏,首页 [B][C] 单栏堆叠,课表可横向滚动且不错版。
12. 删除/清空/导入覆盖均弹 M4 二次确认;M1 标题留空提交时红字提示且不保存。
